//! AST-level pass tests have been removed along with the
//! `frontend::solidity::analysis` module, which was superseded by the
//! `analysis` crate. Pass infrastructure tests now live in `crates/analysis/`.
